#include "main.h"
/**
 * _abs - this convert all numbers to postive
 * @n: int type number
 * Description: Functions Nested loops
 */

int _abs(int absolute)
{
	if (absolute < 0)
	{
		return (absolute * -1);
	}
	else
	{
		return (absolute);
	}
}
